import { styled } from "@mui/material"

const Highlights = () => {
    const StyledDiv = styled("div")(() => ({
        height: "100vh",
    }))
    return (
        <StyledDiv>
            <p>Produtos tops</p>
        </StyledDiv>

    )
}

export default Highlights